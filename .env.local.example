NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-side operations only. Do not expose publicly.
SUPABASE_SERVICE_ROLE_KEY=

# API rate limiting (Upstash Redis REST). Optional locally — limits disabled if unset.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
